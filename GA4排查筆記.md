# GA4 即時報表看不到資料 — 排查筆記

> 適用：潮旅國際旅行社官網
> 測量 ID：`G-47DV1VPF9J`
> 網站：https://web-production-83d7b.up.railway.app/

---

## 排查對照表

| # | 症狀／可能原因 | 怎麼確認 | 解決方式 |
|---|--------------|---------|---------|
| 1 | **代碼還沒部署完成** | 看網站原始碼是否含 `G-47DV1VPF9J` | 等 Railway 部署完（約 1 分鐘）後重整網站 |
| 2 | **瀏覽器快取舊版頁面** | 重整後仍看不到 | 按 **Ctrl + Shift + R** 強制清快取重整 |
| 3 | **廣告攔截外掛擋掉 GA**（最常見） | 關掉 AdBlock/uBlock 再測 | 改用**無痕視窗**或**手機**開網站測試 |
| 4 | **測量 ID 不一致** | 比對網站代碼 vs GA4 資料串流的 ID | 兩者必須完全相同（本站為 `G-47DV1VPF9J`） |
| 5 | **看錯報表期間** | 確認看的是「即時」不是「參與度」 | 報表 → **即時（Realtime）**，只顯示近 30 分鐘 |
| 6 | **根本沒有真人造訪** | 即時報表需要實際瀏覽觸發 | 自己用瀏覽器開網站、停留並捲動幾秒 |
| 7 | **GA4 帳號剛建，尚在初始化** | 頂端顯示「48小時內安裝」提示 | 標準報表最多延遲 24–48h，但**即時**應立即顯示 |
| 8 | **看錯 GA4 資源/帳戶** | 左上角資源名稱是否為「website / 潮旅國際」 | 切換到正確的資源 |
| 9 | **網路或防火牆擋 Google** | 換網路（如手機 4G）測試 | 公司防火牆可能擋 googletagmanager.com |
| 10 | **同意聲明模式擋資料**（歐洲訪客） | 台灣訪客通常不受影響 | 一般台灣營運可忽略 |

---

## 🎯 最快驗證法（3 步）

1. **手機**開 https://web-production-83d7b.up.railway.app/ （避開電腦的廣告外掛與快取）
2. 在頁面上捲動停留約 10 秒
3. 電腦 GA4 看 **報表 → 即時** → 應出現「使用者：1」

> 💡 90% 的「看不到資料」都是 **#2 快取** 或 **#3 廣告攔截**。
> 先用無痕視窗或手機測，幾乎都能解決。

---

## 諮詢轉換事件設定

網站已在表單成功送出時觸發 GA4 事件 `generate_lead`，
帶有參數：行程（tour_interest）、交通（transport）、人數（people）、是否候補（is_waitlist）。

**建議設定為關鍵事件（轉換）：**
1. GA4 → **管理 → 事件**
2. 找到 `generate_lead`，把右側「標記為關鍵事件」開啟
3. 之後即可在報表看到諮詢轉換率，也能接 Google Ads 衡量廣告成效

> 注意：事件要先有人實際送出一次表單後，才會出現在事件清單中。

---

## 日後若綁定自訂網域（phbay.net）需同步更新

以下位置目前都指向 Railway 網址，換網域時要一起改：

- `index.html` — `<link rel="canonical">`、Open Graph `og:url` / `og:image`、Twitter `twitter:image`、JSON-LD 的 `url` / `image`
- `robots.txt` — `Sitemap:` 那一行
- `sitemap.xml` — `<loc>` 網址
- GA4 資料串流的網址、以及本筆記內的網址
